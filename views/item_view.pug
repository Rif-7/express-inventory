extends layout 

block content 
  div.item_field
    h1= item.name
    a(href=item.categorie.url) #{item.categorie.name}
    hr
    p= item.description 

    ul 
      li Price:  #{item.price}
      li Stock:  #{item.stock}
    
    a(href=item.url+'/update') Edit